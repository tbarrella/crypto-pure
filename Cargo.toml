[package]
name = "crypto-pure"
version = "0.0.9"
authors = ["Taylor Barrella <tbarrella@gmail.com>"]
edition = "2018"

[dependencies]
byteorder = { version = ">=1.3.0", default-features = false }

[features]
default = ["std"]
std = []
